# Keep captures local first

SnipMac processes captures locally and never sends them off the Mac unless the user deliberately chooses a sharing destination. This privacy boundary keeps the core workflow trustworthy and account-free; future cloud or collaboration features must preserve explicit user control rather than turning upload into an implicit step.
